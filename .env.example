# Kopiere diese Datei als .env.app und trage den echten Wert ein.
# Bcrypt-Hash generieren: php -r "echo password_hash('DEIN_PASSWORT', PASSWORD_BCRYPT, ['cost'=>12]), PHP_EOL;"
# In .env.app fuer Docker Compose alle $ im Hash als $$ schreiben.
ADMIN_PASSWORD_HASH=hier_bcrypt_hash_eintragen
